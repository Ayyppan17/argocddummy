apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: nginx-helm-multienv
  namespace: default
spec:
  generators:
    - list:
        elements:
          - name: demo
            namespace: demo
            url: https://kubernetes.default.svc
          - name: np
            namespace: np
            url: https://aksnp-dns-dag8atyj.hcp.eastus.azmk8s.io:443
  template:
    metadata:
      # each generated Application will be named: <name>-nginx
      name: '{{name}}-nginx'
    spec:
      project: default
      # Helm chart source (Bitnami public Helm repo)
      source:
        repoURL: 'https://charts.bitnami.com/bitnami'
        chart: nginx
        # targetRevision can be pinned if required
        targetRevision: 22.5.5
        helm:
          values: |
            replicaCount: 2
            service:
              type: ClusterIP
      destination:
        server: '{{url}}'
        namespace: '{{namespace}}'
      # optional: enable automated sync with prune + selfHeal for the demo
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
